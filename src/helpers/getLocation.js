/**
 * Joins the strings by a comma and returns a concatenated string.
 * @param {string} city - City string to be passed for address.
 * @param {string} country - Country string to be passed for address.
 */
export default function(city, country) {
  return `${city}, ${country}`;
}
